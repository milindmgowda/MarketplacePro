import '../dist/index.js';

// This file serves as the entry point for Vercel's serverless functions
// It just imports the already bundled index.js file from the dist directory
// This approach allows the static files to be served from the dist directory
// while still having the API routes handled by the serverless function